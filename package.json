{
  "name": "solar-estimate",
  "version": "1.0.0",
  "description": "Solar energy estimator with automated roof detection and energy projections",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["solar", "energy", "estimator", "roof-detection"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
